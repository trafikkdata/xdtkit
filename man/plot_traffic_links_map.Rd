% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_traffic_links_map}
\alias{plot_traffic_links_map}
\title{Create Interactive Map of Traffic Links}
\usage{
plot_traffic_links_map(
  directed_predictions,
  color_by = "uncertainty_category",
  heavy_vehicle = FALSE,
  balanced = TRUE,
  palette = "RdYlGn",
  opacity = 0.8,
  weight = 3
)
}
\arguments{
\item{directed_predictions}{A data frame containing prediction data with geometry.
Must be an sf object or convertible to one.}

\item{color_by}{Character string specifying the column name to color links by.
Default is "uncertainty_category".}

\item{heavy_vehicle}{Logical. If `TRUE`, uses heavy vehicle predictions and
uncertainties in popups. If `FALSE` (default), uses total AADT.}

\item{balanced}{Logical. If `TRUE` (default), uses flow-balanced predictions.
If `FALSE`, uses raw INLA predictions.}

\item{palette}{Character string specifying the color palette. For categorical
variables, options include "Set1", "Set2", "Dark2", "Paired". For continuous
variables, options include "YlOrRd", "RdYlGn", "viridis", "plasma". Default
is "RdYlGn" (reversed so low=green, high=red).}

\item{opacity}{Numeric between 0 and 1 controlling line opacity. Default is 0.8.}

\item{weight}{Numeric controlling line width. Default is 3.}
}
\value{
A leaflet map object showing traffic links with interactive popups.
}
\description{
Creates a leaflet map displaying traffic links colored by a specified variable.
Useful for spatial exploration of predictions, uncertainty, and quality control.
}
\examples{
\dontrun{
# Color by uncertainty category
plot_traffic_links_map(predictions, color_by = "uncertainty_category")

# Color by relative uncertainty (continuous)
plot_traffic_links_map(
  predictions,
  color_by = "relative_uncertainty",
  palette = "YlOrRd"
)

# Color by road class
plot_traffic_links_map(
  predictions,
  color_by = "vegkategori",
  palette = "Set2"
)
}

}
