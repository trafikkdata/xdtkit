test_that("data validation works", {
  # Should return no error
  #expect_invisible(validate_input_data(traffic_links, directed_traffic_links, nodes))

  #traffic_links_add <- cbind(traffic_links, new_col = NA)
  #expect_warning(validate_input_data(traffic_links_add, directed_traffic_links, nodes))

  #traffic_links_red <- traffic_links[, 1:20]
  #expect_warning(validate_input_data(traffic_links_red, directed_traffic_links, nodes))

})
